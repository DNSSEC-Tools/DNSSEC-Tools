
README needed

selinux functioning?, particularly rndc loading... s

On rollerd test, run named to enable reloading.

retries on fails, particularly rollerd?

DONUTS not parsing rrsigs correctly?

Trustman: make DS work?

rollerd test script: add running named (e.g. trustman-rollerd test),
so that rndc will run correctly and the return code parsing out can
be gotten rid of.
